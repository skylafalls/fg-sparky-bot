{
  "name": "@fg-sparky/client",
  "description": "Discord bot client side of FG sparky bot",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "version": "0.13.0",
  "scripts": {
    "build": "rolldown -c && pnpm run postbuild",
    "postbuild": "rm -rf ../../dist ../../build && cp -r ../server/node_modules/sqlite3/build/ dist/ ../.."
  },
  "dependencies": {
    "@fg-sparky/server": "workspace:*",
    "@fg-sparky/utils": "workspace:*",
    "bufferutil": "catalog:discord",
    "commander": "catalog:utils",
    "discord.js": "catalog:discord"
  },
  "devDependencies": {
    "comptime.ts": "catalog:utils",
    "vite": "catalog:build"
  }
}
